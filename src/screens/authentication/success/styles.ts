import {StyleSheet} from 'react-native';
import {GlobalScreen} from '../../../configs/GlobalStyles';

export const AddPhotoStyles = StyleSheet.create({
  container: {
    ...GlobalScreen,
    justifyContent: 'center',
    alignItems: 'center',
    gap: 20,
  },
});
